sap.ui.define([
	"sap/ui/core/mvc/Controller",
	"sap/m/MessageBox",
	"sap/m/MessageToast"
], function(Controller, oMsgBox, oMsgTst) {
	"use strict";
	return Controller.extend("mw.controller.view2", {
		/**
		 * Called when a controller is instantiated and its View controls (if available) are already created.
		 * Can be used to modify the View before it is displayed, to bind event handlers and do other one-time initialization.
		 * @memberOf mw.view.view2
		 */
		onInit: function() {
			this.oRouter = this.getOwnerComponent().getRouter();
			this.oRouter.attachRouteMatched(this.bingo, this);
		},
		bingo: function(oEvent) {
			var index = oEvent.getParameter("arguments").myVar;
			debugger; 
			var sPath = "/" + index;
			
			if ( oEvent.getParameters().name === "detail") {
					this.getView().bindElement(sPath); }
		  
					// this.getView().bindElement(sPath);  
				
            // }
			
		},
		onBack: function() {
			var oView1 = this.getView();
			var oApp = oView1.getParent().getParent();
			oApp.toDetail("idView0");
		},
		goSuccess: function() {
			oMsgBox.confirm("Success!!", {
				title: "Confirmation"
			});
		},
		goError: function() {
			oMsgBox.error("Errr...", {
				title: "Error..",
				onClose: function(choice) {
					if (choice === "CLOSE") {
						oMsgTst.show("all good");
					}
				}
			});
		},
		onConfirm: function(oEvt) {
			debugger;
			var val = oEvt.getParameter("selectedItem").getValue();
			sap.ui.getCore().byId(this.idTF).setValue(val);
		},
		idTF: null,
		onF4: function(oEvt) {
				debugger;
				this.idTF = oEvt.getSource().getId();
				var oSelDg = new sap.m.SelectDialog({
					title: "Countries",
					items: {
						path: "/masterSet",
						template: new sap.m.DisplayListItem({
							label: "{Person}",
							value: "{LastName}"
						})
					},
					confirm: [
						this.onConfirm,
						this
					]
				});
				this.getView().addDependent(oSelDg);
				oSelDg.open();
			}
			/**
			 * Similar to onAfterRendering, but this hook is invoked before the controller's View is re-rendered
			 * (NOT before the first rendering! onInit() is used for that one!).
			 * @memberOf mw.view.view2
			 */
			//	onBeforeRendering: function() {
			//
			//	},
			/**
			 * Called when the View has been rendered (so its HTML is part of the document). Post-rendering manipulations of the HTML could be done here.
			 * This hook is the same one that SAPUI5 controls get after being rendered.
			 * @memberOf mw.view.view2
			 */
			//	onAfterRendering: function() {
			//
			//	},
			/**
			 * Called when the Controller is destroyed. Use this one to free resources and finalize activities.
			 * @memberOf mw.view.view2
			 */
			//	onExit: function() {
			//
			//	}
			,
		/**
		 *@memberOf mw.controller.view2
		 */
		anotherF4: function(oEvent) {
			//This code was generated by the layout editor.
		}
	});
});